const { mdLinks } = require('./lib/mdLinks');

module.export = {
  mdLinks,
};

// mdLinks(pathToMarkdown)
//   .then((links) => {
//     console.log(links);
//   })
//   .catch((error) => {
//     console.error(error);
//   });